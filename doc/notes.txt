

C:\Users\mlglenn\.gitconfig

git config --global user.name "Mark L. Glenn"
git config --global user.email "mark.glenn@mybrighthouse.com"

git clone git@git.bhn.net:care-application-development/newman.git newman


Remember these 4 commands:

git add .

git commit -m 

git pull origin

git push origin master		Tr00per$




C:\IntelliJ_Projects\newman_git_repo\newman>git remote -v
origin  git@git.bhn.net:care-application-development/newman.git (fetch)
origin  git@git.bhn.net:care-application-development/newman.git (push)




[user]
               name = Mark L. Glenn
               email = mark.glenn@mybrighthouse.com
[core]
               autocrlf = true
               whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol
[help]
               autocorrect = 1

[color]
               branch = auto
               diff = auto
               status = auto
               ui = auto
               interactive = auto

[color "branch"]
               current = yellow bold
               local = yellow
               remote = green

[color "diff"]
               meta = yellow bold
               frag = magenta bold
               old = red bold
               new = green bold

[color "status"]
               added = yellow
               changed = green
               untracked = cyan

[alias]
               aa = add -A
               ls = log --decorate --pretty=format:'%C(green)%h%Creset [%C(red)%an%Creset] (%C(normal)%ar%Creset) - %s'
               svnr = svn rebase
               svnc = svn dcommit
               clog = svn log
               co = checkout
               cm = "checkout master"
               stat = status
               unstage = reset HEAD
               uncommit = reset --soft HEAD^
               last = log -1 HEAD
               b = branch -v
               r = remote -v
               t = tag -l
               cp = cherry-pick -x
               ribbon = tag --force _ribbon origin/master
               catchup = log --patch --reverse --topo-order _ribbon..origin/master
               
[diff]
               tool = opendiff

[difftool]
               prompt = false

[difftool "opendiff"]
               cmd = /usr/bin/opendiff \"$LOCAL\" \"$REMOTE\" -merge \"$MERGED\" | cat

[merge]
               summary = true

[gc]
               auto = 1

[push]
               default = simple
